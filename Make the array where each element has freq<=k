// here k = 1,2,3,4,5,6,7......
int i=0;
        for(int curr:nums)
        {
            if(i==0 || i==1 || nums[i-2]!=curr)
            {
                nums[i] = curr;
                i++;
            }
      }
return i; // it is the size of the newArray.
